<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vn.edu.huflit.foozie_app" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Required for all versions if need precise location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Required for background location services on Q+ (10) -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <!-- Required for foreground services on P+ (9), this is normal permission -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FOOZIE_APP"
        android:usesCleartextTraffic="true" >
        <activity
            android:name=".DetailsFoodTypeActivity"
            android:exported="false" />
        <activity
            android:name=".LocationActivity"
            android:exported="false" />
        <activity
            android:name=".DetailFoodsActivity"
            android:exported="false" />
        <activity
            android:name=".testActivity"
            android:exported="true" />
        <activity
            android:name=".DetailVoucherActivity"
            android:exported="false" />
        <activity
            android:name=".verifyAccountActivity"
            android:exported="false" />
        <activity
            android:name=".activity_signup"
            android:exported="true" />
        <activity
            android:name=".activity_signin"
            android:exported="true" />
        <activity
            android:name=".intro_screen"
            android:exported="true" />
        <activity
            android:name=".splash_screen"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>